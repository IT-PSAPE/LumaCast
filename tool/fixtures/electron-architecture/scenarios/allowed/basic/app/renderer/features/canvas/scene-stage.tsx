import type { RenderScene } from './scene-types';
import type { DomainThing } from '@core/types';

export function SceneStage(): JSX.Element {
  const scene: RenderScene = {} as never;
  void scene;
  void ({} as DomainThing);
  return null;
}