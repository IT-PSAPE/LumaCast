{
  "name": "@lumacast/protocol",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "Wire-level contracts: the IPC surface, snapshot patches, deck-bundle manifest/inspection shapes, NDI observability, project backup, and the runtime codecs that decode them at trust boundaries."
}
